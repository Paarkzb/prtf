include:
  - ../auth/sso/docker-compose.yml
  - ../quiz/docker-compose.yml

services:
  nginx:
    build:
      context: .
    networks:
      - api_gateway_network
    ports:
      - "80:80"
    deploy:
      restart_policy:
        condition: on-failure

networks:
  api_gateway_network:
